(function(angular) {

	'use strict';

	angular.module('tangelo.company', []).config(Configure);

	Configure.$inject = [ '$routeProvider' ];

	function Configure($routeProvider) {

	}

})(window.angular);
