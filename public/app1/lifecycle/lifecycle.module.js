(function(angular) {

	'use strict';

	angular.module('tangelo.lifecycle', []).config(Configure);

	Configure.$inject = [ '$routeProvider' ];

	function Configure($routeProvider) {

	}

})(window.angular);
