(function(angular) {

	'use strict';

	angular.module('pentaWorkflow.comboasset', []).config(Configure);

	Configure.$inject = [ '$routeProvider' ];

	function Configure($routeProvider) {
	}

})(window.angular);