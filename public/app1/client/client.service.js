(function(angular) {

	'use strict';

	function ClientService($http, $q) {
				
	}

	ClientService.$inject = [ '$http', '$q' ];

	angular.module('pentaWorkflow.client').service('ClientService', ClientService);

})(window.angular);