(function(angular) {

	'use strict';

	angular.module('pentaWorkflow.region', []).config(Configure);

	Configure.$inject = [ '$routeProvider' ];

	function Configure($routeProvider) {
	}

})(window.angular);